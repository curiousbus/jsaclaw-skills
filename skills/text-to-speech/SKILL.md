# 文字转语音

将文本转换为自然语音音频文件。

## 使用场景

当用户要求将文本朗读、转语音、生成音频时使用。

## 参数

- **text** (必填): 要转换的文本内容，最长 5000 字符。
- **voice** (可选): 语音角色，可选: alloy, echo, fable, onyx, nova, shimmer。默认 "nova"。
- **language** (可选): 语言代码，如 "zh", "en", "ja"。不传则自动检测。

## 注意

- 生成的音频格式为 MP3
- 长文本可能需要较长时间，请告知用户耐心等待
